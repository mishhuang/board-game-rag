version: 1.1.5

cache: true

interface:
  appTitle: "Board Game Rules Assistant"

endpoints:
  anthropic:
    apiKey: "${ANTHROPIC_API_KEY}"
    models:
      - claude-sonnet-4-20250514

fileConfig:
  endpoints:
    default:
      fileLimit: 5
      fileSizeLimit: 10
      totalSizeLimit: 50
      supportedMimeTypes:
        - "application/pdf"